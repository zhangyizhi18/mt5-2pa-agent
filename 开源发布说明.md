# 开源发布说明 — mt5-2pa-agent v0.4.4

本文说明本次开源**打包了什么、排除了什么、做了哪些脱敏处理**，以及推送到 GitHub 前后
必须完成的几项安全操作。

> 📌 本文随源码一同发布，因此**刻意不写出任何真实凭据的字面值**，只引用所在文件路径。
> 复现打包器 `tools/pack_opensource.py` 属内部发布工具（它本身就含有待脱敏的原始字面量），
> **留在私有工作目录，不随源码发布**。

---

## 一、结论速览

| 项目 | 状态 |
| :--- | :--- |
| 源码 / 测试 / 提示词 / 前端 | ✅ 完整收录 |
| 真实密钥（LLM API Key） | ⛔ 已排除（`.env`、`.env.bak`） |
| 主机凭据（NAS 账号与明文密码） | ⛔ 已排除（`deploy/`、3 个 tools 脚本） |
| 私人交易数据（217 MB 决策与审计记录） | ⛔ 已排除（`records/`） |
| Web 控制台账号口令哈希 | ⛔ 已排除（`config/users.json`） |
| 编译产物与发行包（5.9 GB） | ⛔ 已排除（`target/`、`release/`、`*.ex5`） |
| 上游原作者版权声明 | ✅ 保留（`LICENSE` + 新增 `NOTICE`） |
| 许可证表述错误（误标 MIT） | 🔧 已修正为 AGPL-3.0-or-later |
| 内网 IP 泄漏 | 🔧 已脱敏为 `192.168.1.100` |
| 出厂自检（结构 + 内容两层） | ✅ 通过 |

---

## 二、收录内容

```
mt5-2pa-agent/
├── src/                    Rust 服务全部源码（45 个 .rs）
├── tests/                  6 个集成测试文件（40 项断言）
├── static/                 Web 控制台（index.html / app.css / app.js）
├── prompt_engineering/     提示词工程与策略库（含 _reference）
├── experience/             经验库目录骨架（仅 .gitkeep）
├── mql5/PABridge.mq5       MT5 桥接 EA 源码
├── docs/                   部署指南、用户手册、本文件
├── config/                 仅 settings.example.json 与 .gitkeep
├── tools/                  可公开的工具（前缀缓存实测、涨跌配色自检）
├── Cargo.toml / Cargo.lock 依赖清单（锁定版本，便于复现构建）
├── Dockerfile / docker-compose.yml / .dockerignore
├── Makefile / start.bat / start.sh
├── .env.example            脱敏配置模板（不含任何真实值）
├── LICENSE                 上游 AGPL-3.0-or-later 原文件（未改动）
├── NOTICE                  来源、版权与许可证义务声明（新增）
├── SECURITY.md             安全策略（已修正）
├── 开源发布说明.md          本文件
└── .gitignore              重新编写，覆盖全部敏感路径
```

---

## 三、排除内容与原因

### 3.1 密钥与运行凭据（最关键）

| 路径 | 内容 | 处理 |
| :--- | :--- | :--- |
| `.env` | 一个**真实可用**的 `LLM_API_KEY`（值已隐去） | ⛔ 不打包 |
| `.env.bak` | 另一个真实 `LLM_API_KEY` + 一个真实 `MT5_BRIDGE_TOKEN` | ⛔ 不打包 |
| `config/settings.json` | 运行态配置，运行时会写入 | ⛔ 不打包，保留 `settings.example.json` |
| `config/users.json` | `admin` / `viewer` 的口令盐值与 SHA-256 哈希 | ⛔ 不打包 |

### 3.2 主机凭据（同样关键）

`deploy/` 整个目录以及 `tools/` 下 3 个脚本，都以硬编码常量形式写入了内网 NAS 的
**IP、SSH 端口、用户名与明文密码**，形如：

```python
HOST, PORT, USER, PWD = "<内网 IP>", 10022, "<用户名>", "<明文密码>"
```

涉及文件：`deploy/` 全部 14 个文件、`tools/equity_dual_preview.py`、
`tools/make_equity_preview.py`、`tools/nas_equity_stat.py`。这些全部排除，
`tools/` 下其余可公开的脚本已收录。

### 3.3 私人数据与体积

| 路径 | 体积 | 原因 |
| :--- | :--- | :--- |
| `records/` | **217 MB / 964 个文件** | 逐根 K 线的 AI 决策记录、`trade_audit.jsonl` 交易审计、权益曲线；属私人交易数据 |
| `target/` | **5.9 GB** | Cargo 编译产物，`cargo build --release` 可复现 |
| `release/` | 12 MB | 已构建的 win64 zip 与 docker tar.gz；二进制产物不进源码仓库 |
| `promo/` | 5.3 MB | 宣传物料，其中 `_nas_live_*.png` 含**实盘账户截图** |
| `demo_video/` | 21 MB | 演示视频配音素材（mp3/wav） |
| `mql5/PABridge.ex5` | 235 KB | MQL5 编译产物，源码已收录 |
| `免费AI注册.txt` | — | 含**个人邀请码**与推广短链，非项目源码 |
| `升级说明.txt`、`启动服务.bat` | — | 面向 EXE 发行包的终端用户文档，不属于源码仓库 |
| `.cargo/config.toml` | — | 本机使用的第三方 crates 镜像源，涉及供应链偏好，对外发布可能引起争议 |
| `tools/pack_opensource.py` | — | 内部发布工具，自身含待脱敏的原始字面量 |

> 说明：以上内容**只是没有被复制进发布包**，工作目录中的原文件一个都没有删除或修改。

---

## 四、已做的脱敏与文档修正

打包器在**输出副本**上执行替换（工作目录原文件不动），全程打印替换日志：

1. **内网 IP 脱敏**：把内网 NAS 的真实地址统一替换为示例地址 `192.168.1.100`
   （命中 `docs/用户手册.md`、`docs/用户手册.html` 里的 EA BridgeUrl 示例）。
2. **修正许可证错误表述**：
   - `README.md` 徽章 `License-MIT` → `License-AGPL-3.0`；
   - `README.md`「开源许可证」章节重写为「开源许可证与来源声明」，写明上游仓库、
     版权方、AGPL-3.0-or-later 与网络服务条款；
   - `docs/用户手册.html` 页脚「遵循其 MIT 许可证」→「遵循其 AGPL-3.0-or-later 许可证」。
3. **修正 `SECURITY.md`**：移除对并不存在的 `tools\setup_git_secrets.ps1` 的引用，
   改为核对 `.gitignore` 与提交前自检；联系方式由「QQ 群（见 README，README 中并无该信息）」
   改为 GitHub Issues / Security Advisories。
4. **新增 `NOTICE`**：声明上游 `oficcejo/2pa-agent-rust`、
   `Copyright (C) 2026 okx-2pa-agent-web Contributors`、AGPL-3.0-or-later，
   并逐条列出本移植版保留了什么、修改了什么（满足 AGPL 的修改标注义务）。
5. **重写 `.gitignore`**：原规则并未忽略 `config/settings.json`、`config/users.json`、
   `records/`、`release/`、`promo/`、`mql5/*.ex5`——`SECURITY.md` 里「已被忽略」的陈述
   与实际不符。新规则把这些路径全部覆盖，作为第二道闸门。

---

## 五、开源前必须完成的安全操作

> 本次发布包含一个**全新 `git init` 的仓库**，历史中没有任何密钥痕迹（这是刻意的：
> 先清空历史再谈脱敏，比事后改写历史可靠得多）。但由于密钥与主机凭据此前一直以明文
> 形式存放于本地工作目录，请仍按下面清单处理。

1. **作废并重建 `.env` 与 `.env.bak` 中的两个 LLM API Key**
   - 在服务商后台删除重建；
   - 重建后只写入本机 `.env`，不要写进任何被 Git 跟踪的文件。
2. **更换 NAS 密码**
   - `deploy/nas_chpw.py`、`deploy/nas_build.py` 等脚本中硬编码的是**明文 SSH 密码**，
     且这些脚本一直在实际使用；
   - 若该主机对外可达，请立即更换，并改用密钥登录替代密码登录。
3. **推送前最后一道自检**（在发布包根目录执行）：
   ```bash
   git status --short                     # 确认无 .env / records/ 等
   git ls-files | grep -iE "\.env|settings\.json|users\.json|records/|deploy/"
   grep -rInE "sk-[A-Za-z0-9]{20,}|192\.168\." . --exclude-dir=.git
   # 再手工把两个凭据字面量加进上面的 grep 模式跑一遍（本文不写出其值）
   ```
   都应当**没有输出**。
4. **在 GitHub 仓库开启防护**：Settings → Code security → 启用
   *Secret scanning* 与 *Push protection*，作为兜底。
5. **不要把 `records/` 加回来**：它属于真实交易数据，不是示例数据。
   若需要给使用者演示，请另建 `examples/` 并人工构造。

---

## 六、推送到 GitHub

发布包内已经完成 `git init` 与首次提交，开箱即可推送：

```bash
cd mt5-2pa-agent
git log --oneline                 # 应只有一次初始提交，历史中不含密钥

# 在 GitHub 上新建一个空仓库（不要勾选 Add README / .gitignore / license）
git remote add origin https://github.com/<你的用户名>/mt5-2pa-agent.git
git push -u origin main
```

仓库创建后建议补齐：

| 设置项 | 建议值 |
| :--- | :--- |
| Description | `MetaTrader 5 AI 日内交易代理 · Rust + PABridge EA · 移植自 oficcejo/2pa-agent-rust (AGPL-3.0)` |
| Topics | `mt5` `metatrader5` `rust` `mql5` `quantitative-trading` `price-action` `llm` `agpl` |
| License 识别 | 仓库会自动识别 `LICENSE`，确认显示 `AGPL-3.0` |
| About → Website | 填你的用户手册或文档地址（可选） |
| Releases | 建议**不要**把 `release/` 里的 exe/zip 当源码包上传；如需发行二进制，另建 Release 并附 SHA-256 |
| Branch protection | main 分支保护 + 禁止强制推送 |

---

## 七、许可证与合规要点

- 本项目与上游同为 **AGPL-3.0-or-later**（SPDX 见 `LICENSE`）。**不能改成 MIT**：
  上游采用强 copyleft 许可证，派生作品必须沿用同一许可证。
- **必须保留** `LICENSE` 中的原版权行 `Copyright (C) 2026 okx-2pa-agent-web Contributors`
  与本包新增的 `NOTICE`。
- **网络服务条款**：若你把本服务部署到公网供他人使用（哪怕只是给同事用），
  也必须向使用者提供修改后的完整源码。本项目的 Web 控制台正是「通过网络交互」的形态，
  这一条适用。
- 风险免责声明已在 `README.md` 保留，且 EA 需人工输入 `ENABLE LIVE` 才能实盘。
  开源时**不要**删除该声明。

---

## 八、如何为下一个版本重新打包

打包器留在**私有工作目录**的 `tools/pack_opensource.py`：

```bash
# 1) 修改 Cargo.toml 里的 version（打包器据此命名产物）
# 2) 在项目根目录执行
python tools/pack_opensource.py

# 可选参数
python tools/pack_opensource.py --out "D:/workbuddy/2pa-agent-rust/dist"   # 输出根目录
python tools/pack_opensource.py --no-git                                  # 只出目录不建 Git
python tools/pack_opensource.py --no-zip                                  # 不打压缩包
```

打包器自带三道保护：

1. **显式白名单**——不在名单里的新文件默认不会被收集，避免新增的密钥/日志被顺手带出去；
2. **文本脱敏**——内网 IP 与许可证表述替换，逐条打印日志；
3. **出厂自检（两层）**——
   - *结构检查*：输出目录内不允许出现 `.env*`、`config/settings.json`、`config/users.json`、
     `records/`、`deploy/`、`target/`、`release/`、`promo/`、可执行产物等，判断无歧义；
   - *内容检查*：只匹配「真实泄漏」特征（长格式 `sk-` 密钥、已知主机账号与密码、
     桥接令牌值、私钥头、非示例内网 IP），文档里的 `sk-你的密钥`、测试夹具等占位符不误报。
   - **任一层命中即中止并返回退出码 2**，不会产出带密钥的压缩包。

输出目录与压缩包位于**项目之外**（默认 `../dist/`），不会污染工作目录。

---

## 九、验收自检表

- [ ] `dist/mt5-2pa-agent-src-v0.4.4.zip` 已生成，解压后 `cargo test` 可跑通
- [ ] 包内搜索 `sk-` 长串、NAS 用户名与密码字面量、真实内网 IP 均无结果
- [ ] 包内存在 `LICENSE`（AGPL 原文）与 `NOTICE`，且版权行未改动
- [ ] `README.md` 许可证徽章显示 AGPL-3.0，不再是 MIT
- [ ] `config/` 下只有 `settings.example.json` 与 `.gitkeep`
- [ ] 没有 `records/`、`target/`、`release/`、`deploy/`、`promo/`、`demo_video/`
- [ ] `.gitignore` 已覆盖 `.env`、`config/users.json`、`records/`
- [ ] 已作废重建两个 LLM API Key，已更换 NAS 密码
